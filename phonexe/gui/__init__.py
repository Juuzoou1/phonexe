"""
Desktop GUI for phonexe (PyQt6).

A dark, bilingual (Arabic / English) forensic dashboard that drives the same
extraction backend used by the CLI. Launch with `phonexe gui`.

PyQt6 is an optional dependency; the CLI works without it.
"""
